const shoppingCartReducerDefaultState=[]

const shoppingCartReducer = (state=shoppingCartReducerDefaultState,action)=>{
    switch(action.type){
        default:
            return state;
    }
}

export default shoppingCartReducer
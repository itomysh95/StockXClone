import React from 'react'
import {connect} from 'react-redux'

const NewLowestAsk =()=>(
    <div>
    </div>
)

const mapStateToProps=(state)=>{
    return{
        
    }
}

export default connect(mapStateToProps)(NewLowestAsk)
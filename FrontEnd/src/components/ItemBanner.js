import React from 'react'
import {connect} from 'react-redux'

// banner for switchin between Sneaker view, Street wear view, collectibles view, handbags view, watches view
// when clicked switch view to display current selected 
const ItemBanner = ()=>(
    <div>
        
    </div>
)

const mapStateToProps=(state)=>{
    return{

    }
}

export default connect(mapStateToProps)(ItemBanner)